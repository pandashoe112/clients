---
// Delete this file once the real sections are in. It exists to show the shape:
// a plain .astro file using the class names already defined in base.css, with an
// anchor id that matches a nav item in Sanity.
//
// For images, put them in src/assets/images/ and render them with astro:assets so
// they are converted to WebP and resized at build time:
//
//   import {Image} from 'astro:assets'
//   import photo from '../assets/images/photo.jpg'
//   <Image src={photo} alt="..." widths={[520, 1040]} sizes="520px" loading="lazy" />
---

<section class="about" id="why">
  <div class="wrap">
    <div>
      <span class="eyebrow">Why choose us</span>
      <h2 style="margin-top:16px">Replace this section</h2>
      <p class="lead" style="margin-top:16px">
        Everything below the hero is this site's own code. The hero, header, SEO and
        business details all come from Sanity.
      </p>
      <div style="margin-top:32px"><a class="btn btn-ink" href="#quote">Get a free quote</a></div>
    </div>
  </div>
</section>
